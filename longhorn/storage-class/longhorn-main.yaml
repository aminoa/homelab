apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: longhorn-main
provisioner: driver.longhorn.io
parameters:
  numberOfReplicas: "2"
  dataLocality: "best-effort" # prefer scheduling pods near their data
reclaimPolicy: Retain
volumeBindingMode: Immediate
